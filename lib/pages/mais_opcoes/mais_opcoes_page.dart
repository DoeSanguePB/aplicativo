import 'package:doe_sangue_pb/utils/widgets.dart';
import 'package:flutter/material.dart';

class MaisOpcoesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Mais Opções"),
      ),
      body: Center(
        child: TextBlack("Mais Opções"),
      ),
    );
  }
}
