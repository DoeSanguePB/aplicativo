import 'package:doe_sangue_pb/utils/widgets.dart';
import 'package:flutter/material.dart';

class LocaisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Locais"),
      ),
      body: Center(
        child: TextBlack("Locais"),
      ),
    );
  }
}
